#include <stdio.h>
#include <limits.h>

int main(int argc, char const *argv[])
{
	int stdio=standard input output header title;
	printf("Define full Form");
	scanf("%d", &a);
	a=stdio;
	printf("Full Form of stdio is = %d", a);
	return 0;
}